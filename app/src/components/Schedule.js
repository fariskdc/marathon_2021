import React from "react";
import Calendar from "./Calendar";

const Schedule = () => {
  return (
    <div className="calendar">
      <h1>Calendar</h1>
      <Calendar />
    </div>
  );
};

export default Schedule;
